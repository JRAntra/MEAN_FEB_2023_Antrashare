import { DisplayCharPipe } from './display-char.pipe';

describe('DisplayCharPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCharPipe();
    expect(pipe).toBeTruthy();
  });
});
