import { WelcomeMessageDirective } from './welcome-message.directive';

describe('WelcomeMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new WelcomeMessageDirective();
    expect(directive).toBeTruthy();
  });
});
